/**
 * @file main.cpp
 * @brief Точка входа серверного приложения казино "Хампстерс"
 * @author Development Team
 * @date 2025
 * @version 1.0
 * 
 * Этот файл содержит функцию main() для серверной части приложения
 * казино "Хампстерс". Инициализирует консольное Qt приложение и
 * запускает TCP-сервер для обработки клиентских подключений.
 */

#include <QCoreApplication>
#include "mytcpserver.h"

/**
 * @brief Главная функция серверного приложения
 * @param argc Количество аргументов командной строки
 * @param argv Массив аргументов командной строки
 * @return Код завершения приложения
 * 
 * Инициализирует Qt Core приложение (без GUI), создает экземпляр
 * MyTcpServer и запускает главный цикл обработки событий.
 * Сервер начинает прослушивание порта для входящих TCP-соединений
 * от клиентов казино.
 * 
 * Приложение работает в консольном режиме и обрабатывает:
 * - Подключения клиентов
 * - Авторизацию и регистрацию пользователей
 * - Игровые операции и обновление балансов
 * - Административные команды
 */
int main(int argc, char *argv[])
{
    QCoreApplication a(argc, argv);
    
    // Создание и запуск TCP-сервера для обработки клиентов казино
    MyTcpServer myserv;
    
    return a.exec();
}
