/**
 * @file main.cpp
 * @brief Главный файл серверного приложения казино
 * @author Команда разработчиков
 * @date 2025
 * @version 1.0
 * 
 * Содержит точку входа в серверное приложение казино "Hampsetes".
 * Сервер обрабатывает подключения клиентов и управляет игровыми сессиями.
 */

#include <QCoreApplication>
#include "mytcpserver.h"

/**
 * @brief Главная функция серверного приложения
 * @param argc Количество аргументов командной строки
 * @param argv Массив аргументов командной строки
 * @return Код завершения приложения
 * 
 * Инициализирует Qt приложение, создает экземпляр TCP сервера
 * и запускает главный цикл обработки событий для приема
 * подключений клиентов казино.
 */
int main(int argc, char *argv[])
{
    QCoreApplication a(argc, argv);
    MyTcpServer myserv;
    return a.exec();
}
