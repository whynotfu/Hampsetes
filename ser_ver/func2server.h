/**
 * @file func2server.h
 * @brief Заголовочный файл функций обработки запросов сервера
 * @author Команда разработчиков
 * @date 2025
 * @version 1.0
 * 
 * Содержит объявления функций для обработки различных типов
 * запросов от клиентов казино к серверу.
 */

#ifndef FUNC2SERVER_H
#define FUNC2SERVER_H

#include <QByteArray>
#include <QString>
#include <QDebug>
#include <QTcpSocket> // для работы с сокетом

/**
 * @brief Парсинг входящих запросов от клиентов
 * @param request Строка с запросом от клиента
 * @return Обработанный ответ в виде массива байт
 * 
 * Анализирует входящий запрос от клиента, определяет его тип
 * (авторизация, регистрация, статистика и т.д.) и вызывает
 * соответствующую функцию обработки.
 */
QByteArray parsing(const QString& request);

/**
 * @brief Обработка запроса авторизации
 * @param login Логин пользователя
 * @param password Пароль пользователя
 * @return Результат авторизации в формате протокола
 * 
 * Проверяет учетные данные пользователя в базе данных
 * и возвращает результат авторизации с информацией о пользователе.
 */
QByteArray Auth(const QString& login, const QString& password);

/**
 * @brief Обработка запроса регистрации
 * @param login Логин нового пользователя
 * @param password Пароль нового пользователя
 * @param email Email адрес пользователя
 * @return Результат регистрации в формате протокола
 * 
 * Создает новый аккаунт пользователя в системе после
 * проверки уникальности логина и email.
 */
QByteArray Registration(const QString& login, const QString& password, const QString& email);

/**
 * @brief Получение личной статистики пользователя
 * @return Статистика текущего пользователя в формате протокола
 * 
 * Возвращает персональную игровую статистику:
 * баланс, количество игр, выигрыши, проигрыши.
 */
QByteArray MyStats();

/**
 * @brief Получение статистики всех пользователей (только для админов)
 * @return Полная статистика всех пользователей в формате протокола
 * 
 * Возвращает расширенную статистику по всем зарегистрированным
 * пользователям. Доступно только администраторам.
 */
QByteArray AllStats();


#endif // FUNC2SERVER_H
