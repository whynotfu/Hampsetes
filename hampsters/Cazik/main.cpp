/**
 * @file main.cpp
 * @brief Главный файл приложения казино "Hampsetes"
 * @author Команда разработчиков
 * @date 2025
 * @version 1.0
 * 
 * Этот файл содержит точку входа в приложение казино. Приложение представляет собой
 * игровую платформу с различными мини-играми: слоты, рулетка, кости, кликер и игра "больше-меньше".
 */

#include "managerform.h"

#include <QApplication>

#include <QSqlDatabase>

#include <QSqlQuery>

#include "client_functions.h"

/**
 * @brief Главная функция приложения
 * @param argc Количество аргументов командной строки
 * @param argv Массив аргументов командной строки
 * @return Код завершения приложения
 * 
 * Инициализирует Qt приложение, создает главный менеджер форм,
 * устанавливает начальный баланс игрока и запускает главный цикл событий.
 */
int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    
    // Инициализация класса ManagerForm - в нем находятся классы всех окон
    // При инициализации создаются новые экземпляры классов окон (регистрации, меню, мини-игр...)
    ManagerForm w;

    // Установка начального баланса игрока
    robuks = 1000;

    return a.exec();
}
