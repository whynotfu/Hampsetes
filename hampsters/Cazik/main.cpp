/**
 * @file main.cpp
 * @brief Точка входа клиентского приложения казино "Хампстерс"
 * @author Development Team
 * @date 2025
 * @version 1.0
 * 
 * Этот файл содержит функцию main() для клиентской части приложения
 * казино "Хампстерс". Инициализирует Qt приложение и запускает
 * менеджер форм для управления пользовательским интерфейсом.
 */

#include "managerform.h"
#include <QApplication>
#include <QSqlDatabase>
#include <QSqlQuery>
#include "client_functions.h"

/**
 * @brief Главная функция клиентского приложения
 * @param argc Количество аргументов командной строки
 * @param argv Массив аргументов командной строки
 * @return Код завершения приложения
 * 
 * Инициализирует Qt приложение, создает экземпляр ManagerForm
 * и запускает главный цикл обработки событий. ManagerForm
 * координирует все окна и формы приложения казино.
 * 
 * Устанавливает начальный баланс пользователя в 1000 робуксов
 * для демонстрационных целей.
 */
int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    
    // Инициализация класса ManagerForm - центрального координатора всех окон
    // При инициализации создаются экземпляры всех форм (авторизация, меню, мини-игры)
    ManagerForm w;

    robuks = 1000; // Установка начального демонстрационного баланса

    return a.exec();
}
